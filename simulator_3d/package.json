{
  "name": "planets3d-simulator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --no-coverage",
    "test:verbose": "jest --no-coverage --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
